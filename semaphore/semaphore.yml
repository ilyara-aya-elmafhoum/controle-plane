commands:
  - checkout
  - cd repository_1_template_1/terraform
  
  # Voir TOUTES les variables d'environnement
  - echo "=== TOUTES les variables d'environnement ==="
  - env
  
  # Voir spécifiquement les TF_VAR_
  - echo "=== Variables TF_VAR_ ==="
  - env | grep TF_VAR_
  
  # Voir les variables sans préfixe
  - echo "=== Autres variables ==="
  - env | grep -E "(OPENSTACK|OS_|sysadmin|devops|terraform|vm_|floating|admin)"